enum ParseState: String {
        case none = "抓取微博视频"
        case parsing = "查看已抓取视频"
    }
    
    fileprivate var parseState: ParseState = .none

enum ParseState: String {
        case none       = "抓去微博视频"
        case parsing    = "查看已抓去视频"
    }

    fileprivate var parseState: ParseState = .none